name: Java CI with Maven

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Build with Maven
        run: mvn -B package
        working-directory: my-app

      - name: Set up Node 14
        uses: actions/setup-node@v3
        with:
          node-version: 14

  opensource-security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run Snyk Open Source Security Check
        uses: snyk/actions/maven@master
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

  # code-security:
  #   needs: [build]
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v2
  #     - name: Run Snyk Code Security Check
  #       uses: snyk/actions/maven@master
  #       env:
  #         SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
  #       with:
  #         command: code test

  # release:
  #   needs: [build]
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v2
  #     - name: Set up JDK 17 for Release
  #       uses: actions/setup-java@v1
  #       with:
  #         java-version: 17
  #     - name: Set Git user
  #       run: |
  #         git config user.email "ankitanagarale09@gmail.com"
  #         git config user.name "ankitanagarale"
  #     - name: Change directory to my-app
  #       run: cd my-app
  #     - name: Publish JAR
  #       run: mvn -B release:prepare release:perform -DskipTests
  #       env:
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
